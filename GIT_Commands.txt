git --version
git config --global user.name
git config --global user.name

git clone https://github.com/Azure/azure-sdk-for-net

git init

git status

git add Demo1.txt

git status

git commit -m "Changes Commited"

git revert in case we need to change the commit message 


git remote add origin